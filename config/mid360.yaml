/**:
    ros__parameters:
        map_file_path: "./PCD/"
        common:
            lid_topic:  "/livox/lidar"
            imu_topic:  "/livox/imu"
            time_sync_en: false         # ONLY turn on when external time synchronization is really not possible
            time_offset_lidar_to_imu: 0.0 # Time offset between lidar and IMU calibrated by other algorithms, e.g. LI-Init (can be found in README).
                                        # This param will take effect no matter what time_sync_en is. So if the time offset is not known exactly, please set as 0.0
            # 重定位
            reloc_en: true       # true: use priori global map

        # 重定位启动时的 GICP 初始粗对齐：用第一帧扫描与先验地图配准，
        # 自动校正 initialPose 的水平误差（x/y/yaw），缓解“初始位姿偏差大导致飘飞”。
        # 仅在 reloc_en 且 initialPose.use 都为 true 时生效，且只在启动跑一次。
        icp_align:
            use: true            # true: 启动时做 GICP 粗对齐校正初始位姿
            accum_frames: 5      # 累积多少帧成稠密点云再配准(=1 即单帧)；累积期间跳过EKF更新
            max_iterations: 50   # GICP 最大迭代次数
            max_corr_dist: 1.0   # 最大对应点距离(m)，初值误差大可适当调大
            fitness_thresh: 0.5  # GICP fitness 分数阈值；超过则视为配准失败，保持原始 initialPose
            voxel_leaf: 0.2      # source/target 配准前的体素降采样尺寸(m)，0 表示不降采样

        preprocess:
            lidar_type: 1                # 1 for Livox serials LiDAR, 2 for Velodyne LiDAR, 3 for ouster LiDAR, 
            scan_line:  4
            blind: 0.1
            timestamp_unit: 3
            scan_rate: 10
            body_filter: true # 是否滤除车体
            body_radius: 0.33
            body_height: 0.7

        mapping:
            acc_cov: 0.1
            gyr_cov: 0.1
            b_acc_cov: 0.0001
            b_gyr_cov: 0.0001
            fov_degree:    360.0
            det_range:     450.0
            extrinsic_est_en:  false      # true: enable the online estimation of IMU-LiDAR extrinsic
            extrinsic_T: [ -0.011, -0.0234, 0.044 ]
            extrinsic_R: [ 1., 0., 0.,
                            0., 1., 0.,
                            0., 0., 1.]

        initialPose:
            use: false  #blue=true,red=false
            position:
                x: 0.000 #0.07
                y: 0.000 #-0.235
                z: 0.000 #0.45
            orientation:
                w: 1.000 #0.656
                x: 0.000 #0.265
                y: 0.000 #0.276
                z: 0.000 #-0.651

        lidar_extrinsic: #body2lidar
            position:
                x: 0.0
                y: 0.0
                z: 0.75
            orientation:
                w: 0.000
                x: 1.000
                y: 0.000
                z: 0.000            
        
        publish:
            path_en: true                # true: publish Path
            effect_map_en: false         # true: publish Effects
            map_en: true                # true: publish Map cloud
            scan_publish_en: true       # false: close all the point cloud output
            dense_publish_en: true       # false: low down the points number in a global-frame point clouds scan.
            scan_bodyframe_pub_en: true  # true: output the point cloud scans in IMU-body-frame

        pcd_save:
            pcd_save_en: false
            interval: -1                 # how many LiDAR frames saved in each pcd file; 
                                        # -1 : all frames will be saved in ONE pcd file, may lead to memory crash when having too much frames.
